/**
 * Глобальные лимиты пагинации.
 * Используются в PaginationQueryDto и в утилите parseLimit.
 * Единый источник правды — если лимит меняется здесь, меняется везде.
 */

export const DEFAULT_PAGE_SIZE = 20;

/**
 * Жёсткий верхний предел размера страницы для любых публичных/админских списков.
 * Защита от DoS и неограниченных выборок из БД.
 */
export const MAX_PAGE_SIZE = 100;

export const DEFAULT_PAGE = 1;
